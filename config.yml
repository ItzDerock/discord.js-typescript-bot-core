# Discord Bot Core
# This file must be placed in the same folder as where you run `npm start` or `node .` (for ./dist)

token: "your token"
prefix: "your prefix"

# =====================================
# =           STYLE OPTIONS           =
# =====================================
embeds:
  primary:
    color: "#548CFF"
  error:
    color: "#F14A16"